rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // each user can read/write only their own flights
    match /users/{uid}/flights/{flightId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
